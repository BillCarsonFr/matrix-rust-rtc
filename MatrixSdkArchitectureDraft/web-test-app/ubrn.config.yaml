rust:
  directory: ..
  manifestPath: Cargo.toml
web:
  wasmCrateName: matrix-rtc-wasm-shim
  ts: src/generated
  manifestPatchFile: Cargo.patch.toml
  features:
    - uniffi
    # test-only: runtime assumptions probe (test/runtimeProbe.test.ts)
    - runtime-probe
